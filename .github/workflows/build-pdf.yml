name: Build PDF
on:
  schedule:
    - cron: '0 0 * * *'
  push:
    branches:
      - master
jobs:
  build-pdf:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2
        with:
          persist-credentials: false
      - name: Install Fonts
        run: |
          sudo apt-get install -y fonts-liberation
          sudo apt-get install -y fonts-cmu
      - name: Install & Update TexLive
        shell: bash -l {0}
        run: |
          sudo apt-get -qq update
          sudo apt-get install -y     \
            texlive-latex-recommended \
            texlive-latex-extra       \
            texlive-fonts-recommended \
            texlive-fonts-extra       \
            texlive-xetex             \
            latexmk                   \
            xindy
      - name: Build Math-Tambahan PDF
        shell: bash -l {0}
        run: |
          pip install jupyter-book
          jb build math-tambahan/ --builder pdflatex
      - uses: actions/upload-artifact@v2
        with:
          name: pdf
          path: math-tambahan/_build/latex/book.pdf
